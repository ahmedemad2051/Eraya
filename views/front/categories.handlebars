<div class="container pt-4">

    <div class="row">

        <div class="jumbotron list-content col-sm-8 p-2 m-auto">

                <div class="table-responsive">
                    <table class="table">

                            {{#each categories}}
                            <tr class="pl-4">
                                    <td style="text-align: center"><a href="/category/{{_id}}" style="color: black;">{{name}}</a></td>
                            </tr>
                            {{/each}}

                    </table>
                </div>

        </div>

    </div>
</div>